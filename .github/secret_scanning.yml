# Test fixtures hold synthetic credentials in the formats real providers use: that is
# what the secrets detector is tested against. Nothing under tests/ is a live secret.
paths-ignore:
  - "tests/**"
